import 'package:flutter/material.dart';

class WinnersTable extends StatelessWidget {
  const WinnersTable({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Column(),
    );
  }
}
